/* Extend the Request interface in place to include your custom variable */
declare global {
    namespace Express {
        interface Request {

        }
        interface Response {

        }
    }
}

export {}